package tiled;

/**
 * Represents all possible types of properties. For now only STRING is supported.
 */
public enum PropertyType {
    BOOL,
    COLOR,
    FLOAT,
    FILE,
    INT,
    OBJECT,
    STRING
}
